CREATE TABLE Employees (
    ID INT,
    Name VARCHAR(50),
    Salary INT
);
INSERT INTO Employees VALUES
(1, 'Chetan', 50000),
(2, 'Rahul', 70000),
(3, 'Priya', 60000),
(4, 'Ankit', 70000);
SELECT Name,
       Salary,
       RANK() OVER (ORDER BY Salary DESC) AS RankNo
FROM Employees;
